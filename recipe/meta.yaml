{% set name = "opsdroid" %}
{% set version = "0.14.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: b3bdf92bababb6bf5e31ea9d279626a69e7e9d624d5e773cc5ee66bc28efccd4

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv"
  entry_points:
    - opsdroid = opsdroid.__main__:main

requirements:
  host:
    - python >=3.5
    - babel
    - pip
  run:
    - python >=3.5
    - aiohttp
    - aioslacker
    - arrow
    - babel
    - click
    - nbconvert
    - nbformat
    - pycron
    - pyyaml
    - websockets
    - appdirs
    - multidict

test:
  imports:
    - opsdroid
    - opsdroid.parsers
    - opsdroid.database
    - opsdroid.connector
    - opsdroid.connector.slack
    - opsdroid.connector.websocket
  commands:
    - opsdroid --help


about:
  home: https://opsdroid.github.io/
  license: APACHE-2.0
  license_family: APACHE
  license_file: LICENSE
  summary: 'An open source ChatOps bot framework.'

  description: |
    An open source chat bot framework written in python. It is designed to be extendable, scalable and simple.
  doc_url: https://opsdroid.readthedocs.io/
  dev_url: https://github.com/opsdroid/opsdroid

extra:
  recipe-maintainers:
    - FabioRosado
    - jacobtomlinson
    - synapticarbors
